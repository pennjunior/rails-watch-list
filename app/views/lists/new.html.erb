<div class"custom-background-img">
</div>
  <%= simple_form_for(@list) do |f| %>
    <div class="container text-center w-50">
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.submit "Create List", class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>
